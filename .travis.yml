sudo: required

language:
    - c

services:
    - docker

before_install:
    - docker pull liberforce/gtk3:mageia6

script:
    - docker run liberforce/gtk3:mageia6 /bin/sh -c "pwd && mkdir builddir && meson builddir && ninja -C builddir && ninja -C builddir install"
